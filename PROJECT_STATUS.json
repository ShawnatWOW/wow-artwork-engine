{
  "_meta": {
    "purpose": "Fast-load context for the WOW Artwork Engine. Read this first each session to know where we are without re-deriving from code. Keep updated as the project moves.",
    "lastUpdated": "2026-08-10",
    "humanReadableStatus": "STATUS.md (this file's plain-English companion — full changelog + current state)",
    "recentWork": [
      "2026-08-10b: STITCHING DELETED + AGGRESSIVE MOTION — the dormant two-segment chain was removed entirely (every piece = ONE Seedance call; a 2.0 revert yields a 15s piece via fal.js duration clamp, never a splice). New buildSpectacularArcPrompt: single three-movement time-beat prompt (ignition ~10s / metamorphosis ~12s / crescendo ~8s), boilerplate once; new designs store it as motion_prompt with motion_prompt_act2=null (legacy rows keep act2; combineSpectacularActs bridges at animate time). Aggression: standing frame-burst rule, near-camera flybys/scale surges, ambient particle layer, explosive slam-into-formation ending (end_image still anchors landing), EON journeys accelerate in surges, EON single loses the smooth-and-hypnotic brake. catalog `segments` renamed `acts`; GEN_EON_DURATION_S=30 opts EON into native 30s (default off). 108/108 tests.",
      "2026-08-10: SEEDANCE 2.5 — motion model moved to bytedance/seedance-2.5/image-to-video (Shawn: 'go directly into using 2.5'). Native 30s single pass: the spectacular's two acts render in ONE call (combined act prompts, approved closing still as end_image_url) — no more 2x15s chain + handoff frame + ffmpeg stitch. Chain kept as automatic fallback: orchestrator compares duration*segments to providers.motion.maxClipS (2.5=30, 2.0 slugs=15). 2.5 on fal is 480p/720p ONLY (no 1080p; fal.js downgrades a stale 1080p env override) at $0.0214/1k tokens (~$0.4622/s @720p 16:9) — new '2.5' tier in falPricing. Per video: 30s spectacular ~$16.20, 15s EON ~$8.15, one-per-sign batch ~$32.50.",
      "2026-07-25: EON SPINES — the signs were being built face-only. Every EON pod has a narrow LED spine down its LEFT side (WOW template sheet: 3 spines + 3 faces per pod, face 256x384, spine 64x384). Aspect + cutting strategy rebuilt around the real wrapped geometry: a pod slab = spine+face = 320 native (1600 at 4K), 3-pod master 4800x1920 (gen 4096x1638, 5:2), single pillar now its OWN 1600x1920 master (gen 3200x3840, 5:6) instead of a bare face. eonSlicer is layout-driven (computePanelCrops) and cuts spine+face per pod; artworks.panel records which panel ('pod2_spine'); one fal call is split across panels by width via orchestrator.allocateCost (exact, remainder on last). Prompts gained wrap-band language (color flows through the fold, focal detail stays out). Dashboard renders true pod geometry (1:4 spine:face, corner tight, pods spaced) + a WrapGuide overlay on un-cut designs. Migration 007. Key: a pod slab is EXACTLY one third of the 3-pod master, so the 3-act choreography still maps one act per pod.",
      "2026-07-24: Keep & explore — anchor a liked design; variations rail (re-roll + plain-language LLM tweak); original never lost; promote. First LLM in the engine (services/generation/tweak.js, OpenAI, graceful fallback to re-roll). Lineage: family_id/parent_artwork_id/change_note; keeper = selections row. LIVE (PR #234).",
      "2026-07-22: Perf + UX overhaul — LazyVideo (in-view only; was 9+ 4K streams on load), optimistic per-card actions, skeletons, guided 1-2-3 stepper, 'N reviewed' chips. LIVE (PR #233).",
      "2026-07-22: Save-through-regen + cross-run Sent-to-Jeff history (GET /api/deliveries). LIVE (PR #232).",
      "2026-07-21: Real 3D pop-out — black frame painted INTO the art (subject bursts through), composite letterbox removed. Per-design regen + live progress + failure banners. LIVE.",
      "2026-07-19..21: Accurate itemized cost model (fal token formula) + immutable per-generation ledger + cross-project reconciliation (GET /api/cost/reconciliation; FAL_ADMIN_KEY set). LIVE (PR #230).",
      "2026-07-14..15: 4K pipeline (Seedance 1080p + Topaz 2x upscale) + scene-wide motion overhaul + per-sign regeneration + 15s clips. LIVE."
    ],
    "testCount": "engine 108/108 (server/), offline; frontends build clean",
    "maintainer": "shawn@wowmedia.com",
    "repo": "github.com/ShawnatWOW/wow-artwork-engine",
    "productionRepo": "wow-contract-query (the Artwork Engine tab lives inside WOW's existing dashboard)"
  },

  "project": {
    "name": "WOW Artwork Engine",
    "oneLiner": "Automated weekly AI artwork for WOW Media's billboard network — generate, human-approve, auto-deliver.",
    "goal": "Every week: generate original AI artwork for the signs, Scott (WOW CEO) reviews/approves on a dashboard, approved videos auto-ship to Jeff (the Projectionist) via Google Drive + email.",
    "people": {
      "shawn": "Owner/operator (you). shawn@wowmedia.com",
      "scott": "WOW CEO — reviews and approves artwork on the dashboard",
      "jeff": "Projectionist — receives approved videos (Google Drive folder + Gmail notification)"
    },
    "tracking": {
      "linear": "dllmlabs/Delivery, issues DEL-138..143",
      "slack": "via Linear channel link"
    }
  },

  "status": {
    "overall": "LIVE IN PRODUCTION, fully featured. Generate → review (keep/explore/tweak) → animate → send to Jeff all shipped. Iterating on art direction and sign geometry.",
    "liveSince": "2026-07-11 (Artwork Engine tab on wowautomation.ai / wow-contract-query dashboard)",
    "generationMode": "live in production (spends real fal.ai money); fixture ($0) locally",
    "tests": "88/88 passing as of 2026-07-25 (offline; LLM + providers injectable)",
    "currentFocus": "EON spines shipped 2026-07-25 — needs visual confirmation on a fresh LIVE batch that the wrapped composition reads (art continues across the corner; nothing important lands on a spine band).",
    "milestones": {
      "M0_foundations": "done",
      "M1_generation": "done",
      "M2_review_dashboard": "done",
      "M2.5_two_phase_stills_then_video": "done",
      "M3_delivery_google_drive_gmail": "done — shipped + live, with cross-run sent history (GET /api/deliveries)",
      "M4_first_live_run": "done — live since 2026-07-11; 4K pop-out, accurate costs, per-design regen, keep & explore all shipped",
      "M5_scheduler": "built but SCHEDULER_ENABLED=false; turn on once you want hands-off Mondays"
    }
  },

  "architecture": {
    "principle": "Deploy INTO WOW's existing AWS infra (same as the Broken News project). NOT a new stack.",
    "frontend": "React tab inside wow-contract-query dashboard (wcq/src/pages/ArtworkEngine.tsx). Standalone dev shell at web/src/App.jsx mirrors it.",
    "backend": "Node/Express in server/. Deployed on EC2 via PM2 (dev + master boxes).",
    "database": "Postgres via pgRepo when DATABASE_URL is set; production currently runs WITHOUT DATABASE_URL, so it uses memoryRepo with JSON snapshot persistence (var/storage/state.json). State survives PM2 restart/deploy.",
    "storage": "Generated media on disk (var/storage) in prod; S3 driver available. Both media and state.json are preserved across 'git reset --hard' deploys.",
    "deployFlow": "push to main (this repo) OR push to wow-contract-query dev -> CI -> staging; PR dev->master + merge -> production. Empty commit triggers redeploy.",
    "email": "Gmail API via Google service account with domain-wide delegation (gmail.send). Sends AS a real @wowmedia.com person (no SMTP)."
  },

  "pipeline": {
    "phase1_stills": "Seedream v4 text-to-image (~$0.03/still). 9 options per batch = 3 per surface. Cheap style gate — Scott approves before any video spend.",
    "phase2_video": "Seedance 2.5 image-to-video runs ONLY on approved stills. 720p (2.5 has no 1080p on fal), native 30s single pass for the spectacular (three-movement arc prompt; closing still = end frame), then Topaz 4x upscale to 4K-class. Graceful fallback to the un-upscaled clip if Topaz fails. NO stitching exists anywhere; a 2.0 revert clamps clips to 15s.",
    "qaGate": "Luma readability check on stills for outdoor visibility (blocks <25 or >200 luma; warns 25-55 with 'qa:' prefix, non-blocking).",
    "delivery": "Approved videos -> Google Drive folder + Gmail notification to Jeff. Offline fallback writes .eml + local files and reports NOT sent.",
    "eonGeometry": "CRITICAL (2026-07-25): an EON pod is a WRAPPED surface — a narrow LED spine down its LEFT side plus its face, both visible to drivers. Panel-native: face 256x384, spine 64x384, so a pod slab is 320x384 (1600x1920 at our 5x 4K scale). Art is generated as ONE continuous panorama across the slabs and CUT into the panels, so it wraps the corner. A slab is exactly 1/3 of the 3-pod master, which is why the 3-act 'one act per third' choreography still maps to one act per pod. Panel order L->R within a pod is spine, then face.",
    "surfaces": {
      "spectacular": {
        "style": "frame_break",
        "spec4k": "3840x1062",
        "gen": "4096x1132 (21:9)",
        "note": "WOW signature 3D frame-break look. The black border is now PAINTED INTO the art by the prompt (subject bursts through it); the ffmpeg composite letterbox was removed 2026-07-21."
      },
      "eon_connected": {
        "style": "eon_connected",
        "spec4k": "eon_master_3pod 4800x1920 -> cut into 6 panels: pod{1,2,3}_spine 320x1920 + pod{1,2,3}_face 1280x1920",
        "gen": "4096x1638 (5:2)",
        "note": "ONE wide master, subject choreographed to travel across the 3 pods (pod-to-pod illusion). 3-act journey rotates weekly. Billed as ONE Seedance call despite 6 panel rows — the bill is split across them by panel width (orchestrator.allocateCost)."
      },
      "eon_single": {
        "style": "eon_single",
        "spec4k": "eon_master_pod 1600x1920 -> cut into pod1_spine 320x1920 + pod1_face 1280x1920",
        "gen": "3200x3840 (5:6)",
        "note": "Standalone single pillar. Gained its own spine 2026-07-25 — a lone pillar still has one on the real structure, so it is composed to wrap and cut exactly like a connected pod (was: a bare 2:3 face)."
      }
    }
  },

  "config": {
    "defaults": {
      "GEN_DURATION_S": 15,
      "GEN_FPS": 30,
      "FAL_RESOLUTION": "1080p",
      "FAL_UPSCALE": "on (Topaz, factor 2)",
      "OPTIONS_PER_SURFACE": 3,
      "COST_STILL_USD": 0.03,
      "COST_VIDEO_PER_SECOND_USD": 0.10,
      "SCHEDULER_ENABLED": false,
      "WEEKLY_RUN_CRON": "0 9 * * 1 (Mondays 9am)"
    },
    "pingpong": "REMOVED 2026-07-16 from spectacular + eon_single. Previously doubled 15s clips to 30s for seamless looping; user found 30s unnecessary. eon_connected never used it (directional travel)."
  },

  "costModel": {
    "_verified": "2026-07-19 against fal model pages. Decision: keep full 4K quality (Scott/Shawn).",
    "sharedFalAccount": "CRITICAL: the fal account is shared across WOW Artwork + WOW Content Automation + WOW Broken News. fal's billing API (GET api.fal.ai/v1/account/billing?expand=credits and /v1/account/focus) reports ALL THREE COMBINED and cannot isolate artwork. Our per-generation estimate (spend.js) is the ONLY artwork-specific figure. Use fal's account total only as an all-projects ceiling.",
    "realPrices": {
      "still_seedream_v4": "$0.03 per image (flat)",
      "seedance_2.5_720p": "~$0.46 per raw second at 16:9 (token formula H*W*24/1024 * $0.0214/1000; 480p/720p only)",
      "seedance_2.0_standard_1080p": "~$0.68 per raw second (rate $0.014/1k — fallback slug)",
      "seedance_2.0_fast_720p": "~$0.30 per raw second",
      "topaz_upscale_above_1080p": "$0.08 per second (our 2x pass to 4K)"
    },
    "perVideoApprox": "Seedance 2.5 @720p + Topaz 4K: 30s spectacular ~$13.80 + $2.40 = ~$16.20; 15s EON master ~$6.95 + $1.20 = ~$8.15.",
    "perFullBatchApprox": "9 stills ($0.27) + videos. One option per surface animated (3 videos) = ~$32.50. Every option animated = ~$97.",
    "history": "The earlier dashboard pill under-reported ~7.6x because it used a blended $0.10/sec. Fixed 2026-07-19 to itemized real rates.",
    "canonicalPriceBook": "server/src/services/generation/falPricing.js is now the single source of truth, encoding fal's EXACT token formula (tokens = w*h*dur*24/1024, cost = tokens/1k * rate1k; standard $0.014/1k, fast $0.0112/1k). Pixel-accurate for any aspect (our billboards are 21:9 / 2:3, never 16:9, so flat per-second rates were always wrong). Mirrored verbatim into wow-contract-query/server/modules/pricing/falPricing.js — KEEP IN SYNC.",
    "immutableLedger": "Each artwork row now stores fal_request_id, upscale_request_id, and cost_usd computed at generation time (where both the 1080p Seedance render dims and the 4K final dims are known). EON's one Seedance call is split across the PANEL rows it produced (6 for a connected set, 2 for a single pillar), weighted by panel width with the remainder on the last, so the ledger sums EXACTLY to the call — spend.js prices a call by summing its rows (grouped by s3_key_raw), so that exactness matters. spend.js prefers cost_usd; falls back to the canonical estimate for pre-ledger rows. No more retroactive re-pricing.",
    "crossProjectReconciliation": "The fal account is billed by TWO projects: WOW Artwork + WOW Content Automation (NOT Broken News — it's OpenAI/Gemini). Both now use the identical canonical price book. Content Automation's falai.js estimateCost had the Seedance /fast tier at $0.022/s vs the real $0.2419/s (~11x under-report) — FIXED, which auto-corrects its billed_usd. Reconciliation lives in wow-contract-query: GET /api/cost/reconciliation?month= federates the artwork ledger (proxied /api/spend) + content-automation billed_usd + fal FOCUS account total (needs FAL_ADMIN_KEY), and reports attributed vs unattributed. Modules: server/modules/pricing/{falPricing,falAccount,reconcile}.js. Dashboard visual (artifact): https://claude.ai/code/artifact/7fbd1551-702d-44c1-9a60-8e3f4e19c31c",
    "estimateSource": "server/src/services/spend.js — sums the immutable per-generation cost_usd ledger (falls back to the canonical falPricing estimate for old rows). Reconciled against fal's account total (all fal projects) as a ceiling.",
    "billingRules": [
      "Itemized: stills (Seedream) + Seedance raw-seconds (rate by tier from model string: '2.5'->$0.0214/1k, 'fast'->$0.0112/1k, else standard $0.014/1k; pixels from the requested resolution) + Topaz seconds (only when model string contains '+topaz').",
      "Fixture rows (model starts with 'fixture' or no model) are free/excluded.",
      "A motion row bills a Seedance call ONLY if it produced raw output (s3_key_raw set). EON connected = 3 rows sharing one raw key -> billed once. Rows with no raw key are skipped (never completed a billable generation).",
      "Seconds = row's RAW generation length (duration_s). Ping-pong retired 2026-07-16 so duration_s == raw for current runs.",
      "Rows filtered to the current calendar month (created_at, falling back to run week_of).",
      "QA-failed stills still count (generated on Seedream, then gated).",
      "Superseded stills (retired by per-sign regen) STILL count — they were generated and did cost money."
    ],
    "falBillingApi": {
      "balance": "GET https://api.fal.ai/v1/account/billing?expand=credits  (Authorization: Key <ADMIN_KEY>) -> current_balance USD. Account-wide (all 3 projects).",
      "focusReport": "GET https://api.fal.ai/v1/account/focus?source=invoice|estimate&start=ISO&end=ISO -> FOCUS CSV. Account-wide, 90-day lookback for estimates."
    }
  },

  "userPreferences": {
    "art": [
      "Psychedelic, bright, saturated, dynamic — NOT abstract/plain (rejected 'plain silver ball moving left to right').",
      "Characterful creatures as subjects (serpent, koi, octopus, peacock, jellyfish, orchid, butterfly, comet), not generic shapes.",
      "MOTION IS THE POINT: full-scene active motion on every artwork, not subtle. Backgrounds must move too. Subjects travel across the whole screen with 3D depth (in/out, vertical, diagonal).",
      "3D depth must be built into the creative PROMPT so the subject genuinely interacts with spatial planes — do NOT rely on a post-processed black letterbox/frame the subject can't break through.",
      "Showcase the full motion capability of the screens.",
      "4K output — these are giant billboards."
    ],
    "ux": [
      "Plain, non-technical language for Scott. Button labels must be obvious to a first-time viewer ('Use this design', 'Approve video', 'Pass', 'Try again').",
      "Clear visual feedback during long operations (spinner/overlay while making videos; 'Making videos... a few minutes each').",
      "Per-sign regeneration, not all-or-nothing (regenerate just Spectacular, or just EON single, etc.).",
      "Show month-to-date spend at the top of the dashboard.",
      "Videos long enough to feel substantial (moved 6s -> 15s), but 30s was too long."
    ],
    "process": [
      "Wants to operate production via the Claude browser extension / AWS console when possible (drop-in JSON, guided clicks), not always CLI.",
      "Wants durable status/context files so we don't waste tokens re-establishing position each session (this file).",
      "Ship to production readily — 'Scott won't test it until it's there anyways.'"
    ]
  },

  "problemsEncountered": [
    {"issue": "Images not loading on dashboard", "cause": "VITE_API_URL='/' + '/api/...' produced '//api/...' protocol-relative URL", "fix": "strip trailing slashes in both API clients", "status": "fixed"},
    {"issue": "'Make video' looked frozen / unresponsive", "cause": "polling race — 202 sent before run.status set to 'running', so dashboard saw stale 'complete'", "fix": "set 'running' BEFORE onStart callback; added spinner overlay + disabled buttons during animation", "status": "fixed"},
    {"issue": "Artwork disappeared on refresh/deploy", "cause": "in-memory repo lost all state on process restart", "fix": "JSON snapshot persistence (atomic tmp+rename) in memoryRepo; hydrate on boot; fail interrupted 'running' rows", "status": "fixed"},
    {"issue": "Art too simple ('plain silver ball left to right')", "cause": "generic subjects + straight-line motion", "fix": "characterful creatures + 8 dynamic 3-act choreographies; psychedelic/bright themes", "status": "fixed then further amplified"},
    {"issue": "Regenerate forced ALL signs", "cause": "single global regenerate", "fix": "per-surface regenerateStills() route + per-section buttons; unapproved marked 'superseded' and hidden; seed salted per attempt", "status": "fixed"},
    {"issue": "Videos too short", "cause": "6s default", "fix": "GEN_DURATION_S 6 -> 15", "status": "fixed"},
    {"issue": "Not 4K for billboards", "cause": "specs + Seedance tier too low", "fix": "SPECS bumped to 4K; Seedance 1080p standard + Topaz 2x upscale; frame/inset scale with canvas width", "status": "fixed"},
    {"issue": "Motion extended to 30s + minimal actual motion", "cause": "ping-pong doubling; prompts described subject-only motion which Seedance under-renders", "fix": "removed ping-pong (back to 15s); rewrote prompts to describe whole-SCENE motion (backgrounds churn, every pixel active)", "status": "shipped 2026-07-16, awaiting visual confirmation"},
    {"issue": "3D frame-break not convincing", "cause": "relied on ffmpeg-drawn black border/letterbox; subject can't truly break through a post-processed frame", "fix": "moved 3D into the still + motion PROMPTS (the black border is painted INTO the scene, subject overlaps it); ffmpeg frameBreakComposite removed", "status": "fixed 2026-07-21"},
    {"issue": "EON artwork was built face-only — the LED spine on each pod's left side was never generated", "cause": "the original catalog modelled a pod as a flat face (master = 3 x face); the spec sheet's spine panel was seeded in the specs table but never produced", "fix": "re-cut the EON geometry around the real wrapped surface: pod slab = spine + face, masters widened (3-pod 4800x1920, single pillar its own 1600x1920), layout-driven slicer emits spine + face per pod, prompts gained wrap-band language, dashboard renders true pod geometry", "status": "shipped 2026-07-25, awaiting live visual confirmation"},
    {"issue": "System could not send artwork to team; spend not visible", "cause": "Google creds missing; spend strip not wired", "fix": "spend strip shipped; Google Drive + service-account JSON configured in S3/.env 2026-07-16", "status": "creds in place; end-to-end test send NOT yet verified"},
    {"issue": "Total spend shows 'over $3K' (2026-07-18)", "cause": "under investigation — see openQuestions", "fix": "TBD", "status": "open"}
  ],

  "openItems": [
    "NEXT: verify the EON wrap on a fresh LIVE batch — does the art read as continuous across the corner, and does anything important land in a spine band? The prompt keeps focal detail out of those bands, but only a real Seedream/Seedance render proves it.",
    "RESOLVED 2026-07-21: 3D frame-break is now prompt-painted; the ffmpeg frameBreakComposite letterbox was removed.",
    "Verify Google delivery end-to-end with the new panel filenames (WOW_<week>_eon_<style>_pod2_spine_320x1920_<id>.mp4) — confirm Jeff can tell spine files from face files.",
    "RESOLVED 2026-07-19: the 'over $3K' figure is the shared fal ACCOUNT total (all 3 WOW projects), not artwork. Our spend formula rebuilt to itemized real rates; artwork-only is a fraction. See costModel.",
    "DEL-143 (user-owned): rotate leaked google-drive-server-account.json.",
    "Turn on SCHEDULER_ENABLED once art + delivery are signed off."
  ],

  "openQuestions": {
    "why_spend_over_3k": {
      "summary": "The dashboard number is an ESTIMATE from server/src/services/spend.js, computed from every LIVE generation THIS CALENDAR MONTH — not a real fal.ai bill.",
      "likelyDrivers": [
        "All of the project's live art-direction iteration landed in one month (July 2026). Each 'too simple' / 'psychedelic' / '4K' / 'motion' round triggered full live regenerations across all surfaces and options, and each is summed into the single month figure.",
        "Rate was bumped to $0.10/raw-second (4K path) and is applied RETROACTIVELY to all historical videos, including ones actually generated on the old $0.04 720p tier.",
        "Duration grew 6s -> 15s (2.5x more seconds per video, all billed).",
        "Per-sign regenerations pile up: superseded stills still count, and any errored/retried video rows may be counted even if they produced no usable output.",
        "REGRESSION I introduced 2026-07-16: removing ping-pong emptied PINGPONG_STYLES, so historical 30s ping-pong rows (frame_break/eon_single) are now billed at 30s instead of the 15s raw — doubling those."
      ],
      "howToPinpoint": "Hover the spend pill — the tooltip shows 'N designs ($X) + M videos / Ss ($Y)'. If M/S is absurd it's a count/duration bug; if the rate*seconds is the driver it's the retroactive 4K rate. The definitive source is production var/storage/state.json (or the real fal.ai dashboard).",
      "proposedFix": "Make the estimate honest: (a) exclude motion rows that failed with no output, (b) split historical vs current per-second rates, (c) stop retroactively re-pricing old tiers, (d) optionally scope the strip to 'this week' not 'this month'. Confirm against the real fal invoice before trusting either number."
    }
  },

  "keyFiles": {
    "server/src/services/orchestrator.js": "runWeek (stills), animateRun (video), regenerateStills (per-sign). Guardrails before spend; QA luma gate; EON slice; frame-break composite.",
    "server/src/services/generation/prompts.js": "THEMES, CHOREOGRAPHIES (8 3-act journeys), SOLO_MOTIONS, buildStillPrompt/buildMotionPrompt. Whole-scene motion + 3D-plane language lives here.",
    "server/src/services/generation/catalog.js": "SPECS (4K dims), SURFACES, planJobs. Source of truth for dimensions.",
    "server/src/services/generation/fal.js": "fal.ai queue logic: Seedance 2.5 (720p, 30s max clip), fal storage upload, Topaz upscale with graceful fallback.",
    "server/src/services/ffmpeg.js": "frameBreakComposite, pingpong, conform, thumbnail, cropdetect. The black-frame border is drawn here.",
    "server/src/services/spend.js": "computeSpend — the MTD estimate behind the $ figure in question.",
    "server/src/services/eonSlicer.js": "layout-driven panel cutter: a wrapped master -> each pod's spine + face (computePanelCrops / sliceMaster, dims derived from catalog SPECS). Serves both the 3-pod set and a single pillar.",
    "server/src/db/memoryRepo.js": "in-memory repo + JSON snapshot persistence (production data store).",
    "server/src/routes/runs.js": "REST: generate, animate, approve/reject, regenerate, /spend.",
    "server/src/config/index.js": "all env-driven config + cost rates.",
    "web/src/App.jsx + web/src/ui.jsx": "standalone dev dashboard (SpendPill, cards, actions).",
    "wcq/src/pages/ArtworkEngine.tsx": "production tab inside wow-contract-query (mirrors App.jsx).",
    "KEYS.md": "credentials checklist (fal, Google Drive folder, service-account JSON)."
  }
}
